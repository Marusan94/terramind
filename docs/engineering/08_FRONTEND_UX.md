# 08 — Frontend / UX

- Tema Cursor oscuro por defecto + Modo Comando alterno (persistido).
- Mapa 3D (MapLibre + terreno + edificios) con 4 capas toggleables que
  SÍ cambian el mapa y el dashboard (`layers.ts` como fuente única).
- Overview AQI flotante: número → categoría → PM → "actualizado hace X" →
  fuente. Minimizable.
- Dashboard overlay con pestañas por capa + Explorar/Calidad/Método.
- Chat ≤60 palabras, viñetas, estructura dato→interpretación→recomendación.
- Accesibilidad pendiente: foco visible, ARIA en toggles, responsive móvil.
