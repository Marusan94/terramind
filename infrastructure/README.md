# ☁️ Terramind Infrastructure & Deployment

This directory contains container definitions and orchestration configurations:

- `docker-compose.yml`: Located at repository root for local development.
- `docker/`: Dockerfiles for `api` and `web`.
- `k8s/`: Kubernetes manifests for scalable production deployments.
